import React from "react";
import Workspace from "../components/Workspace";

export default {
  title: "Components/Workspace",
  component: Workspace,
};

export const Main = {
  render: () => <Workspace />,
};
