import styled from "styled-components";

const CardIcon = styled.div`
  display: flex;
  gap: 10px;
  margin-top: 5px;

  img {
    width: 20px;
    height: 20px;
    cursor: pointer;
  }
`;

export default CardIcon;
