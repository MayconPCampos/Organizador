import styled from "styled-components";

const CardIcon = styled.div`
  display: flex;
  gap: 10px;
  margin-top: 10px;

  img {
    width: 15px;
    height: 15px;
    cursor: pointer;
  }
`;

export default CardIcon;
